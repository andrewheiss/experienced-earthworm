# Things that need to be installed but didn't get installed automatically as 
# dependencies because {renv} couldn't see them
library(quarto)
library(collapse)
library(cmdstanr)
library(yaml)
library(litedown)
